/// Shared [RegExp] and validation patterns (email, phone, SKU, etc.).
///
/// Add entries as features need them; keep patterns here instead of inline.
abstract final class AppPatterns {
  AppPatterns._();
}
